/**
 * TL;DR Javascript code that server injects into pad's HTML page when sending to client
 */
 
//EXAMPLE REQUIRE
//var makeCSSManager = require('./cssmanager').makeCSSManager;
 
function p2pMain() {
	console.log("I'mma fire my lazer!");
}

//Isn't this part just for node.js? We can exclude this right, since this is all just javascript code in the client's HTML page
//exports.someFunction = someFunction;